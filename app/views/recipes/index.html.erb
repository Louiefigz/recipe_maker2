<!-- <header>Recipe Maker V 2.0</header> -->

<div class="container">
  <div class="recipe-index" ng-controller="RecipesController as vm">
    <h2> All Recipes</h2>
    <nav>
      <ul>
      <div ng-repeat="recipe in vm.recipes">
        <li><a href="/recipes/{{recipe.id}}" ng-click="vm.recipeShow(recipe.id)">{{recipe.name}}</a></li>
    </ul>

  </div>
  </nav>
  </div>
  </div>
</div>

<!-- <article>
<div class="ingredient-index" ng-controller="IngredientsController as vm">
  <h2>All Ingredients</h2>
  <div  ng-repeat="ingredient in vm.ingredients">
    <h3 class="right-half">{{ingredient.ingredient}}</h3>
  </div>
</div>
<article> -->
